# Model Zoo

Place pretrained runtime checkpoints here when running `test gmodiff.sh`:

```text
model_zoo/
  dareg_mask.pth
  gmodiff.pkl
```

`dareg_mask.pth` is the frozen stage-2 DaReg checkpoint. `gmodiff.pkl` is the GMODiff checkpoint used for inference.
